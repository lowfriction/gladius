# Sprite tester

Put a sprite file in `images/sprite.png` and open `index.hml` to see it
animated.

The tool will assume that the base tile is square and that frames are
horizontally aligned so it will use the height as the base sprite size.
